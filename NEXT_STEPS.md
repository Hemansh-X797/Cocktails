# 🚀 Project Roadmap & Next Steps

## 📊 1. Visualization & Components

- [ ] **Flavor Profile Radar Chart:** Build/refactor the radar chart component for `flavorProfile` metrics using Recharts/SVG so it can be dynamically colored via CSS/JS.
- [ ] **Card Type Upgrade:** Replace existing default cards across the UI with the custom `cardtype-1` design (reference `.html` template provided by project owner).

## 🍸 2. Content & Data Updates (`src/data/`)

- [ ] **Cocktails Dataset:** Add new recipe entries to `cocktails.json`.
- [ ] **Liqueurs & Crèmes Expansio
- [ ] n:** Add Crème de Cacao, Crème de Menthe, and other essential specialty liqueurs to `spirits.json`.
- [ ] **Tools & Equipment:** Expand `tools.json` with missing bar tools and wire up corresponding image assets in `public/images/tools/`.

## 🧪 3. QA & Build Verification

- [ ] Run `npm run build` locally to verify TypeScript types and static page generation pass cleanly before opening pull requests.+